<?xml version="1.0" encoding="utf-8"?>
<!-- Layout do menu principal com botões de navegação -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Lista de botões centralizada verticalmente -->
    <LinearLayout
        android:id="@+id/linearBotoes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Botão que navega para a tela de Técnicas RPG -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnTecnicas"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_botao_menu"
            android:text="TECNICAS"
            android:textColor="#065271"
            android:textSize="18sp"
            android:textStyle="bold"
            app:backgroundTint="@null" />

        <!-- Botão para acessar a Agenda (em desenvolvimento) -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnAgenda"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_botao_menu"
            android:text="Nao funciona ainda"
            android:textColor="#065271"
            android:textSize="18sp"
            android:textStyle="bold"
            app:backgroundTint="@null" />

        <!-- Botão para acessar Tutoriais (em desenvolvimento) -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnTutoriais"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_botao_menu"
            android:text="Nao funciona ainda"
            android:textColor="#065271"
            android:textSize="18sp"
            android:textStyle="bold"
            app:backgroundTint="@null" />

        <!-- Botão para acessar Check-in (em desenvolvimento) -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnCheckIn"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/bg_botao_menu"
            android:text="Nao funciona ainda"
            android:textColor="#065271"
            android:textSize="18sp"
            android:textStyle="bold"
            app:backgroundTint="@null" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>